"use client";

export type GridlineViewerProps = {
  className?: string;
};

export function GridlineViewer({ className }: GridlineViewerProps) {
  return <section className={className}>Gridline is loading…</section>;
}

